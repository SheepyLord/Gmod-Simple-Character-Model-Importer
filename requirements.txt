# Runtime dependencies for running MMD Character Importer from source
# (the GUI and the step pipeline). Building the Windows executable additionally
# needs requirements-build.txt (which adds PyInstaller).
PySide6==6.11.0
numpy==2.4.4
Pillow==12.1.1
requests==2.32.4
PyOpenGL==3.1.10
